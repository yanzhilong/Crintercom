<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 等待Dialog主题 -->
    <style name="easy_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 后面的activity变暗 -->
    </style>

    <!-- 全屏主题 -->
    <style name="CrFullscreen" parent="AppTheme.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
    </style>

    <!-- 全屏主题 -->
    <style name="CrMain" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@drawable/main_bg</item>
    </style>

    <!-- 全屏主题 -->
    <style name="CrCallingFullscreen" parent="AppTheme.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/cr_bg</item>
    </style>

    <!-- 欢迎界面主题 -->
    <style name="WelcomeEntranceActionBarTheme" parent="AppTheme.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/welcome_bg</item>
 </style>

</resources>